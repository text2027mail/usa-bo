name: Advance Booking Scraper

on:
  schedule:
    - cron: '0 */5 * * *'   # every 5 hours
    - cron: '0 3 * * *'     # 3 AM daily
    - cron: '0 4 * * *'     # 4 AM daily
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code (main repo)
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run advance scraper (with retries)
        env:
          RENDER_SEATMAP_URL: ${{ secrets.RENDER_SEATMAP_URL }}
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          for i in 1 2 3; do
            echo "Attempt $i to run scraper..."
            python usa-advance.py && break
            if [ $i -lt 3 ]; then sleep 10; else exit 1; fi
          done

      # No git commit/push – the script writes directly via GitHub API.
